
````markdown
# 🏦 BankrotBaza Parser — парсер лотов недвижимости

Парсер сайта **bankrotbaza.ru**, предназначенный для сбора информации о лотах недвижимости  
(в первую очередь — **квартиры**) и формирования **удобного Excel-отчёта** с аккуратным оформлением.

Проект автоматизирует рутинный сбор данных с сайта банкротных торгов и приводит информацию к структурированному виду, пригодному для анализа, фильтрации и отчётности.


## 🚀 Возможности

### 🔍 Сбор данных по каждому лоту
Парсер автоматически собирает:

- номер лота  
- название / краткое описание  
- адрес объекта недвижимости  
- начальную цену  
- шаг аукциона  
- размер задатка  
- даты приёма заявок  
- статус аукциона  
- информацию о должнике (наименование, ИНН — если есть)  
- ссылки на документацию (PDF, ZIP и др.)  
- прямую ссылку на лот  


---

## 📊 Excel-отчёт

На выходе формируется файл **Excel (.xlsx)** со следующим оформлением:

- синяя шапка таблицы  
- закреплённая первая строка  
- автофильтры по всем колонкам  
- зебра (чередование строк)  
- перенос текста в длинных ячейках  
- кликабельные ссылки (`Открыть`)  
- аккуратные ширины колонок  
- отсутствие мусорных значений (`Н/Д`)  

Отчёт удобно:
- сортировать по цене  
- фильтровать по региону / должнику  
- использовать для аналитики или отчётности  

---

## 🧰 Стек технологий

- **Python 3.9+**
- **Selenium** — для загрузки динамического контента
- **Google Chrome / ChromeDriver**
- **BeautifulSoup** — парсинг HTML
- **Pandas** — обработка данных
- **XlsxWriter** — форматирование Excel

---

## 📦 Установка

### 1️⃣ Клонирование репозитория

```bash
git clone https://github.com/USERNAME/bankrotbaza-parser.git
cd bankrotbaza-parser
````

### 2️⃣ Создание виртуального окружения (рекомендуется)

```bash
python -m venv .venv
source .venv/bin/activate   # macOS / Linux
# или
.venv\Scripts\activate      # Windows
```

### 3️⃣ Установка зависимостей

```bash
pip install -r requirements.txt
```

Пример `requirements.txt`:

```text
selenium
beautifulsoup4
pandas
xlsxwriter
```

---

## 🌐 Требования

* установлен **Google Chrome**
* доступ к интернету
* Python **3.9 или выше**

> Selenium использует встроенный Selenium Manager, отдельная установка ChromeDriver не требуется.

---

## ▶️ Запуск

```bash
python main.py
```

По умолчанию парсер:

* собирает лоты из категории **квартиры**
* проходит указанное количество страниц
* создаёт Excel-файл в папке проекта

После выполнения в консоли появится сообщение вида:

```text
EXCEL СОЗДАН: /path/to/full_bankrot_report_YYYY-MM-DD_HH-MM-SS.xlsx
```

---

## ⚙️ Настройки

В коде можно изменить:

* количество страниц:

```python
parser.run(max_pages=5)
```

* запуск в headless-режиме (без открытия браузера):

```python
parser = BankrotBazaParser(headless=True)
```

---

## ⚠️ Важные замечания

* Некоторые данные (например, даты торгов) могут отсутствовать в HTML страницы
  → в таком случае они корректно остаются пустыми
* Структура сайта может меняться — селекторы при необходимости нужно обновлять
* Проект предназначен **для учебных и исследовательских целей**

---

## 📌 Возможные доработки

* приведение цен к числовому формату (`₽`)
* парсинг дат как `datetime`
* добавление листа «Статистика»
* поддержка других типов недвижимости
* сохранение в CSV / JSON
* извлечение данных из скрытых API сайта

---



